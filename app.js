const app = new Vue({
  el: '#app',
  data: {
    books: books,
    category: ''
  }
})

// if (category) {
//   book.category === category
// } else {
//   true
// }

// (category) ? book.category === category  : true
